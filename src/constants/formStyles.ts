/**
 * Shared form field classes — single source (Silo v3 / silo-input).
 * Use across wizard and pages; Market Config Tree uses .mct-* instead.
 */
export const wizardMonoInputClass =
  'w-full silo-input silo-input--md font-mono text-sm focus:outline-none focus:ring-0'

export const wizardSansInputClass =
  'w-full silo-input silo-input--md text-sm focus:outline-none focus:ring-0'
